- name: Install and Run nginx #name is not mandatory,this is best practice 
  Host: Mongodb
  tasks:
    - name: install nginx
      ansible.builtin.yum:
        name: nginx
        state: installed
    - name: 
      ansible.builtin.service:
        name: nginx
        state: started